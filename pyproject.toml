[project]
name = "permission-slip"
version = "0.1.0"
description = "Permission Slip v0.1 vertical integration spike (human doctrine over Tethers authority)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.permission-slip]
# No third-party runtime dependencies: the spike uses the standard library and
# talks to the real public tethers.authority/1 stdio seam.
